# Dependencies (will be installed fresh in container)
node_modules
*/node_modules

# Demo bank client: exclude all, keep only package.json for workspace resolution
usecases/demo-bank/client/*
!usecases/demo-bank/client/package.json
# Infrastructure: exclude all, keep only package.json for workspace resolution
infrastructure/*
!infrastructure/package.json

# Git
.git
.gitignore

# IDE/Editor
.vscode
.idea
*.swp
*.swo

# Build artifacts
dist
*.tsbuildinfo

# Environment files (use docker env vars or secrets)
.env
.env.*
!.env.example

# Documentation
*.md
!README.md

# Docker files (avoid recursive copy)
Dockerfile*
docker-compose*
.dockerignore

# Test files
**/*.test.ts
**/*.spec.ts
__tests__
coverage

# Turbo cache
.turbo
